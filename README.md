# MoviePilot-Plugins

MoviePilot 自用插件市场，包含以下插件，可直接作为插件市场源添加：

```
https://github.com/zkmydgth/MoviePilot-Plugins
```

---

## 保种空间守护（SeedSpaceGuard）

存储空间不足时，自动清理保种目录中「保种最久」的资源（种子 + 文件），避免触发 H&R。

| 项目 | 说明 |
| --- | --- |
| 版本 | v1.3.8 |
| 标签 | 存储管理 |
| 适用版本 | MoviePilot >= 2.12.0 |

### 功能特性

- **空间阈值触发**：定期检查卷剩余空间，低于设定阈值时自动开始清理
- **多目录清理**：「保种/清理目录」为多行文本框，**每行填写一个绝对路径**，可同时纳入媒体库目录、保种目录等多个位置；支持 `#` 开头的注释行，自动去重并剔除互为嵌套的目录（同时填 `/a` 与 `/a/b` 时只保留 `/a`）；旧版单目录配置自动迁移，无需重新填写
- **两种清理模式**：
  - 种子级（seed）：直接删除下载器中最老的已完成种子（连带文件），一步到位不留红种
  - 仅文件（file）：仅删除文件，**由本插件自行处理硬链接**，无需再依赖「源文件联动清理」等外部插件
- **硬链接自动双侧删除（仅文件模式）**：按 `(st_dev, st_ino)` 自动识别文件身份，同一 inode 在多目录下的所有副本一次性删净，避免「删一侧留一侧、空间一点没释放」的假删除
- **目标下载器可限定（种子级）**：下拉多选，自动读取 MoviePilot 已启用下载器（qBittorrent/Transmission），弹出选项卡勾选；不选 = 全部
- **空壳种子自动回收**：每轮清理开始前先扫一遍「文件已全部删除、种子却仍留在下载器里做种」的空壳并回收。这类种子源于「单轮只删到预留空间即停」——剩余文件要等后续轮次才删完，而删完那一刻空间往往刚好越过阈值，后续轮次以「空间充足」直接早退，删种判定再无入口。空壳回收**不删除任何文件**，也**不受保护期限制**（文件都没了的种子已无保种价值），仅在「删除种子」开关打开时执行
- **试运行如实预告**：试运行（只列不删）会**照常扫描全部候选种子**，如实报出「这一轮将会回收哪些空壳种子、将会删除哪些文件」，仅不执行真删。预告数与正式跑的实删数严格一致；结果消息一律带 `[试运行]` / `[清理]` 前缀，两种运行模式一眼可辨
- **删到达标为止（按缺口预选）**：按保种时间从早到晚清理，直到剩余空间恢复至阈值以上；真实删除先按当前缺口从旧到新预选文件/种子清单（名义累计 ≥ 缺口），删除后**轮询等待空间实际释放**再复核（等待秒数可配置，默认 90 秒，0 = 不等待），未达标按新缺口继续下一轮
- **空间未释放即停（宁可少删）**：删除后按**字节精度**核对实际释放量，达标即提前结束等待；若空间确实没有释放（可能存在快照引用，或存在不在配置目录内的其它硬链接），则**立即停止本轮清理并告警**，绝不继续叠加删除，避免过量删除不可恢复
- **保护机制**：
  - 最近 N 天添加的种子/文件不清理（保护期，避免误删刚入坑资源）
  - 支持按后缀保护指定文件（如 `*.part`、`*.!qb` 等未完成文件）
  - 自动跳过 DSM 系统目录（`@eaDir` 等）与回收站，只清理真实媒体文件
  - 清理遗留空目录时，同步清除 `@eaDir` 中对应真实文件已不存在的 DSM 索引残片（仅限 `SYNOINDEX`/`SYNO_`/`SYNOFILE`/`THUMB`/`Thumbs.db`/`.DS_Store` 等白名单元数据，避免误删）
  - 删除后自底向上清理遗留空目录，且绝不删除清理目录本身
- **清理范围：除保护后缀外全部删除**：配置目录下**所有文件**均纳入清理候选，不区分文件类型——视频、`.nfo`、海报图片、外挂字幕一视同仁，做到「多余文件一并删除」。「保护文件后缀」是**唯一**的保留手段，需要保留的文件务必填入该项（例如正在下载的 `*.part`、`*.!qb`）
- **DSM 索引残片自动清理**：Synology 会为媒体文件在 `@eaDir` 下生成「同名子目录 + `SYNOINDEX_MEDIA_INFO`」，且不随原文件删除而自动回收，会阻止父目录 `rmdir`，留下「仅剩 `@eaDir`」的空壳目录。插件在清理遗留空目录时会**顺带**清除这些已失去对应真实文件的残片，采用**双重白名单**校验（原文件已不存在 + 目录内仅含 `SYNOINDEX`/`SYNO_`/`SYNOFILE`/`THUMB`/`Thumbs.db`/`.DS_Store` 等已知元数据），任一无关键都会保留整个条目，不会误删 `@eaDir` 下可能存在的用户数据
- **联动清理（可选，默认全关）**：由本插件在删除文件后**主动执行**，无需再装其它插件
  - **联动删除种子**：删除文件后顺带删除对应下载器种子。**仅文件模式下有严格前置条件——必须该种子下的所有文件都已删除，才会删除该种子**；只要还有一个文件在磁盘上（含被保护后缀跳过的未完成文件），种子一律保留。判定以文件**实际存在性**为准，不信任 MoviePilot 的 `state` 字段（该字段不会因本插件删除文件而同步更新）
  - **删除转移记录**：先按目标路径匹配，未命中再按源路径匹配，命中则删除该条转移历史
- **配置错误有提示**：配置中存在不存在的目录时，会在日志与结果消息中明确列出被跳过的路径，不会静默忽略
- **试运行模式**：先预览"将删哪些、预计释放多少空间"，确认无误后再正式清理
- **定时检查 + 手动触发**：支持 cron 定时执行，也支持页面一键试运行/正式清理
- **完成通知**：清理结果通过站内消息推送。定时触发沿用「无动作即静默」（避免每 6 小时推一条「无需清理」的噪音），**手动（页面按钮）与命令（`/seedguard`）触发一律推送结果**——用户主动发起必须有回执；另外定时触发若本轮确实回收了空壳种子，也会通知
- **统计口径一致**：空壳种子回收本质就是「删种」，其数量会计入通知摘要的「删除种子」计数，不会出现「另回收空壳种子 130 个」与「删除种子：0 个」自相矛盾的显示

### 更新历史

<details>
<summary>📜 点击展开更新记录</summary>

| 版本 | 说明 |
| --- | --- |
| v1.3.8 | **三项体验修复**：<br>①**手动触发不再静默**——「空间充足」分支此前硬编码 `notify=False`，会**顶掉**用户已开启的通知配置（`_finish` 只在参数为 `None` 时才回落到用户配置），导致主动点按钮/发命令也收不到任何回执，观感等同「点了没反应」。现改为仅定时触发沿用「无动作即静默」，手动（页面按钮）与命令（`/seedguard`）触发一律推送结果；定时触发若本轮确实回收了空壳种子同样通知。<br>②**统一删种统计口径**——空壳回收本质就是「删种」，但其数量此前只拼进了结果消息正文，**没有计入**通知摘要的「删除种子」字段，于是出现「另回收空壳种子 130 个」与「删除种子：0 个」两个数字并列打架、用户误以为功能未生效。现已计入，并把 `_clean_by_seed` 中两处直接赋值改为累加，避免覆盖已回收的空壳计数。<br>③**精简插件配置表单顶部说明**——原文案 **447 字**一大段挤在首屏，占全表单文案量的 39%，且其中大部分内容（种子级/仅文件、联动清理、保护后缀、硬链接多目录、`@eaDir` 残片）与下方各配置项提示**重复**。现压缩为约 **110 字**，只保留「核心行为 + 安全承诺 + 首次使用引导」，细节全部下沉到对应配置项提示（`@eaDir` 残片清理因无对应配置项，改记入 README 功能特性）。 |
| v1.3.7 | **修复空壳种子误回收（严重误删，务必升级）**：此前判定「文件是否已全部删除」只看下载数据表登记的路径，一旦这些路径**全部失效**（qBittorrent 与 Transmission 并存做种转移、下载器被清空重建、目录迁移等），就会被判成「已删空」并删除种子——**而磁盘上的文件可能完好无损**。实测造成一个 **178GB、84 个文件**的在保种子被误删（记录侧 qB 被清空后，tr 侧仍在正常做种，次日扫描即误判回收）。<br>现改为**增加物理复核**：直接按下载器报告的**真实内容路径**检查磁盘，目录中只要还存在任何文件（含子目录内）即判定「未删空」并保留种子；同时把所有「判不了」的情形（无记录、路径为空、目录不可读、查询异常、条目多到扫不完）**一律收紧为保守保留**，宁可留一个空壳也不误删在保资源；候选路径现已传入判定函数，不再丢失真实路径信息。**联动删种的同源隐患一并修复**。 |
| v1.3.6 | **修复「试运行不可信」**：<br>①**试运行被整段跳过空壳扫描**——`_reap_orphan_seeds` 的首个判断把 `dry_run` 与「无下载器」「未开删种开关」并列为早退条件，导致一进试运行就整段返回。试运行的本意是「如实预告这一轮会做什么」，跳过扫描后给出的「回收 0 个」是假象而非实情（实测：试运行 0.14 秒、正式跑 1.7 秒，差的 1.5 秒就是没去扫种子）。现改为试运行**照常扫描全部候选**，仅把「删种」替换为「记录将回收的种子」，使预告数与正式跑的实删数严格一致；真正的前置阻断只保留「无可用下载器」与「未开启删种开关」两条。<br>②**「空间充足」消息漏拼运行模式标签**——该分支拼接结果消息时未带上已定义好的 `prefix`，导致试运行与正式清理的提示一字不差、无法分辨。现补上 `[试运行]` / `[清理]` 前缀，并让空壳回收数量按运行模式分别显示为「预计回收」与「回收」。<br>③**「空间不足」消息漏报空壳回收量**——空壳回收发生在删文件之前，但只有「空间充足」分支把回收数拼进了结果消息，「空间不足」这条主路径反而完全看不到空壳回收痕迹（回收了却像没回收）。现一并汇总。 |
| v1.3.5 | **修复两个导致「删完文件却不删种子」的缺陷**：<br>①**Transmission 字段命名不兼容（影响面最大）**——插件按 camelCase 取 `percentDone`/`downloadDir`/`totalSize`，而 `transmission_rpc.Torrent` 实际暴露的是 snake_case（`percent_done`/`download_dir`/`total_size`），取值全部落空，种子被判为「未完成」静默丢弃。实测 tr 有 1765 个种子**100% 解析失败**，删种与候选收集整体失效。现按顺序兼容两种命名。<br>②**刮削残留删完不判种**——`.md5`/`.nfo`/`.srt` 等残留不在 `DownloadFiles` 表中，按文件名反查不到 hash，删种判定被整段跳过（连日志都没有）。现新增「按种子目录最长前缀反查」兜底，并在反查失败时打 WARN 留痕。<br>另新增**「空壳种子回收」**：半删种子的剩余文件要等后续轮次才删完，而删完那刻空间往往刚好越过阈值，后续轮次以「空间充足」直接早退，删种判定再无入口，种子永久卡在下载器里。现在每轮开始时无条件扫一次「文件已全部删除但种子仍在」的空壳并回收（不删任何文件，不受保护期限制）。<br>另新增候选收集汇总日志（`下载器共 X 个，未完成 Y 个，不在监控目录 Z 个，纳入候选 W 个`），便于核对「为何只处理了部分种子」 |
| v1.3.4 | **修复「联动删除种子」从未生效的问题（删除种子数恒为 0）**：定位下载器时用了不存在的方法名 `get_torrents`——MoviePilot 的下载器模块方法名是 `list_torrents`，且基类两个都不提供，于是每个下载器都被静默跳过，永远返回「未找到对应的下载器」。现改为按顺序探测 `list_torrents` 与 `get_torrents`，兼容新旧版本；对 `list_torrents` 显式传 `include_all_tags=True`（它默认只返回带 MoviePilot 内置标签的种子，手动添加或转移来的种子会被漏掉），若该版本不接受此参数则自动退回不带参数的调用 |
| v1.3.3 | 诊断增强：定位下载器时不再静默吞掉异常。此前按 hash 查询种子失败时被 `except` 直接吞掉、「未持有该种子」「不在目标下载器范围内」也不留痕，线上只看到一句「未找到 hash 对应的下载器」，无法判断真实原因；现在逐条记录每个下载器未命中的原因（查询异常 / 未持有 / 范围外）并给出汇总 |
| v1.3.2 | 通知格式精简：清理完成后的站内通知只推送三类汇总计数——「删除文件：N 个 / 删除转移记录：N 条 / 删除种子：N 个」（试运行带「预计」前缀，空间未如期释放时附告警行），不再罗列每个被删文件明细，明细全部写入插件日志供自查；Web 配置表单所有字段增加上间距，消除标签与输入框重叠、间距过窄的问题 |
| v1.3.1 | **修正硬链接提示文案歧义**：原「含 N 处硬链接」用的是**路径计数**，措辞却为「处硬链接」（术语上指额外引用数 = 路径数 - 1），容易被误读为「N 个额外链接」或「共 N+1 个文件」。现改为「**含 N 条路径（硬链接，共占 X GB）**」，明确路径条数并说明**同一 inode 只占一份空间**（占用不翻倍）；真实删除分支同步改为「连同其余 N 条路径一并删除」 |
| v1.3.0 | **清理范围改为「除保护文件后缀外全部删除」**：取消「清理刮削文件」独立开关，目录下所有文件（含 nfo/图片/字幕等刮削产物）统一纳入清理候选，不再按文件类型区分，**「保护文件后缀」成为唯一的保留手段**，需保留文件请务必填写该项；同时修复候选池遗漏——此前刮削产物被硬编码排除，导致这类「多余文件」永远清不掉；联动项精简为「联动删除种子」与「删除转移记录」两项 |
| v1.2.0 | **新增三项联动清理**（对齐「源文件联动清理」插件能力，默认关闭，由本插件主动执行不依赖外部插件）：**联动删除种子**——删除文件后删除对应下载器种子，**仅文件模式下严格限定为该种子所有文件均已删除才删种**，任一文件仍在磁盘（含被保护后缀跳过的）即保留种子，判定以文件实际存在性为准而非 `DownloadFiles.state`；**删除转移记录**——按目标路径优先、源路径兜底匹配后删除转移历史；**清理刮削文件**——清理同目录同词干的 nfo/图片/字幕/`.trickplay` 产物，采用紧邻前缀 + 分隔符严格匹配，不会误删同名系列续集或其他媒体；另修正：**刮削产物不再进入按修改时间排序的清理候选池**（体积小、无助于释放空间，且属媒体库元数据） |
| v1.1.0 | **「保种/清理目录」改为多行填写**（每行一个绝对路径，支持 `#` 注释行、自动去重并剔除嵌套目录，旧版单目录配置自动迁移）；**仅文件模式改为按 `(st_dev, st_ino)` 自动识别硬链接并两侧一并删除**，不再依赖「源文件联动清理」等插件联动；**新增字节精度空间释放校验**——删除后轮询等待空间释放（达标提前退出，不再固定空等），若空间确实未释放则立即停止并告警，宁可空间不足也不过量删除；修复配置了不存在目录时仅记录日志、结果消息中无任何提示的问题 |
| v1.0.9 | 新增「联动释放等待秒数」配置项 `sync_wait_seconds`（0-1800，默认 90，0 = 不等待），替代写死的 90 秒；清理空目录时同步清除 `@eaDir` 中对应真实文件已不存在的 DSM 索引残片（仅删白名单元数据，避免误删）；插件简介文案统一（补「可限定目标下载器」） |
| v1.0.8 | 修复真实删除分支引用未定义属性 `_sync_wait_seconds` 导致的 `AttributeError`（补充类属性默认值与 init 复位）；插件说明与 README 明确「删除后等待约 90 秒联动释放再复核空间」 |
| v1.0.7 | 重构真实删除为「按缺口预选清单→删除→等待联动释放→复核空间」多轮策略：删除前先按剩余空间缺口从旧到新预选文件/种子（名义累计≥缺口），删除后等待源文件联动清理释放媒体库侧硬链接再复核空间，达标即停，避免因空间释放滞后导致一次性过量删除 |
| v1.0.6 | 「目标下载器」改为下拉多选：自动读取 mp 已启用下载器弹出选项卡勾选（不选=全部），兼容旧字符串配置 |
| v1.0.5 | 新增「目标下载器」配置：种子级模式可限定处理 qbit/tr（逗号分隔，留空=全部），表单使用说明同步更新 |
| v1.0.4 | 插件详情页新增简略使用说明（清理逻辑/模式/试运行提示） |
| v1.0.3 | 删除种子/文件后自底向上清理遗留空目录（保护清理目录本身不删） |
| v1.0.2 | 删除逻辑改为持续清理保种最久资源直到剩余空间恢复至阈值（移除单次数量上限）；试运行按累计释放量模拟达标；排除 DSM @eaDir 等系统目录 |
| v1.0.1 | 配置页新增手动触发动作（试运行/正式清理），保存即触发并自动复位 |
| v1.0.0 | 初始版本：空间阈值触发、种子级（qB/TR 按添加时间删种子+文件）、仅文件模式、cron 定时、手动触发、试运行与通知 |

</details>

---

## 配置备份（ConfigBackup）

定时备份 MoviePilot 系统配置、数据库及插件配置到指定目录，支持保留数量自动清理、手动触发和一键还原。

| 项目 | 说明 |
| --- | --- |
| 版本 | v1.3.3 |
| 标签 | 系统工具, 备份 |
| 适用版本 | MoviePilot >= 2.0.0 |
| 数据库要求 | MoviePilot 需使用 **PostgreSQL**（10+，含最新 18.x） |

### 功能特性

- **定时自动备份**：按 cron 规则定期备份
- **备份内容**：
  - PostgreSQL 数据库（完整逻辑导出，含表结构、数据、索引与序列）
  - 系统配置文件（app.env、category.yaml、user.db、cookies 等）
  - 插件配置与数据
  - 自定义附加路径（按清单回原路径还原）
- **数据库要求说明**：
  - 数据库备份/还原依赖 MoviePilot 以 PostgreSQL 运行（`DB_TYPE=postgresql`），插件直连主库导出，无需外部 `pg_dump` 工具
  - 兼容 PostgreSQL 10 及以上版本（含 18.x），数据库大版本升级不影响备份与还原
  - 若 MoviePilot 使用非 PostgreSQL（如 SQLite/MySQL），将自动跳过数据库备份与还原，仅备份配置与插件数据（还原时不回写数据库）
- **保留清理**：仅保留最近 N 份备份，旧备份自动清理，避免磁盘膨胀
- **两阶段确认还原**：选中备份 → 页面确认后执行还原，还原前自动先备份当前状态作为安全网，支持中途取消
- **备份目录灵活配置**：支持下拉候选 + 自由输入
- **完成通知**：备份/还原结果站内消息推送

### 更新历史

<details>
<summary>📜 点击展开更新记录</summary>

| 版本 | 说明 |
| --- | --- |
| v1.3.4 | **修复还原操作的两阶段交互**：未选中备份时不再显示【确认还原】按钮（此前点击必然提示「没有待还原的备份」）；列表行的【还原】【删除】按钮补上文字标签，并在未选中状态下给出操作引导 |
| v1.3.3 | **正式发布到插件市场**（启用自动打包发布）：补充插件图标；插件简介补齐「一键还原」能力说明 |
| v1.3.2 | 插件详情页新增简略使用说明（含 PostgreSQL 数据库要求提醒） |
| v1.3.1 | 修正插件作者显示为 zkmydgth |
| v1.3.0 | 备份目录改为下拉选择+自由输入，修复按钮认证参数 |
| v1.1.0 | 新增两阶段确认还原功能 |
| v1.0.0 | 初始版本 |

</details>

---

## 115网盘STRM助手（P115StrmHelper）

> ✅ **已发布**：v3.1.0 已推送并在插件市场上线（`package.v3.json` 中 `release: true`）。
>
> ⚠️ **请勿同时订阅上游仓库**：上游 [DDSRem-Dev/MoviePilot-Plugins](https://github.com/DDSRem-Dev/MoviePilot-Plugins) 的 V2 版插件 ID 同为 `P115StrmHelper`，同时订阅会导致插件被反复覆盖，且两者数据库结构不兼容。
>
> 本插件为 **MoviePilot V3 适配版**，`system_version` 要求 `>=3.0.0`，在 MoviePilot V2 上不会出现在市场列表中——这是版本约束的正常表现，非发布故障。

115 网盘 STRM 生成一条龙服务，覆盖 STRM 生成、302 跳转、转移整理、媒体库同步删除等完整链路。本仓库保存的是 **MoviePilot V3 适配版**（`plugins.v3`），仅支持 MoviePilot >= 3.0.0。

> 说明：本插件移植自上游 [DDSRem-Dev/MoviePilot-Plugins](https://github.com/DDSRem-Dev/MoviePilot-Plugins) 的 V2 版本，在其基础上完成 V3 SDK 适配并同步上游修复。上游暂未提供 V3 版本，故独立维护。

| 项目 | 说明 |
| --- | --- |
| 版本 | v3.1.0（已发布） |
| 标签 | 云盘 |
| 适用版本 | MoviePilot >= 3.0.0 |
| 源码目录 | `plugins.v3/p115strmhelper` |
| 发布状态 | 已发布（`release: true`） |

### 功能特性

- **STRM 生成**：全量 / 增量生成，支持多账号、多目录、媒体库分类
- **302 跳转服务**：Open / Cookie 两种模式，供 Emby / Jellyfin 直连播放；同键并发请求做**单飞合并**，仅向 115 请求一次，避免缓存雪崩
- **多端播放**（账号副本）：同一文件多端同时播放时自动创建副本，规避 115 单文件并发限制
- **转移整理**：接管 MoviePilot 整理流程，支持 115 内部移动/复制、命名识别、失败重试
- **媒体库同步删除**：媒体服务器删除媒体时联动清理网盘文件与 STRM，含目录安全防护
- **FUSE 挂载**、**分享 STRM 清理**、**本地 STRM 备份**、**HDHive 资源获取**等

### 更新历史

<details>
<summary>📜 点击展开更新记录</summary>

| 版本 | 说明 |
| --- | --- |
| v3.1.0 | 同步上游 V2 主线修复：R302 缓存雪崩防护（并发同键只请求一次 115）、增量清理不再误删目录、远程 ffprobe 稳定性增强、302 接口日志降噪并补充异常堆栈；随包携带离线 wheels，测试套件自带宿主桩可独立运行 |
| v3.0.0 | 适配 MoviePilot V3 SDK 与插件依赖清单 |

</details>

### 已知注意事项

- **本插件已发布**（`release: true`，v3.1.0）。因此**不要同时订阅上游**：两者插件 ID 均为 `P115StrmHelper`，同时订阅会导致插件被反复覆盖，且数据库结构不兼容风险高——若此前订阅了上游 V2 版，请先取消订阅再装本版
- 本版本为主版本 3.x，与上游 V2 版本（2.x）不互通，**不建议在 V2 环境安装**
- 在 **MoviePilot V2** 上，因 `system_version` 要求 `>=3.0.0`，该插件不会出现在插件市场列表中。这是版本约束的正常表现，**不是发布故障**
- 插件数据库迁移锚点、事件队列名均沿用上游命名，便于从上游 V2 迁移时保留既有数据

---

## 使用方式

在 MoviePilot「设置 → 插件市场」中添加市场源后，即可搜索安装上述插件。

如果本文档信息与插件实际表现不符，以插件仓库代码为准。
